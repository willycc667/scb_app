import 'package:flutter/material.dart';

class NoHeaderWidget extends StatelessWidget {
  const NoHeaderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 40);
  }
}
